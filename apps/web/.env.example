# Copie para .env.local. Os valores locais saem de `npx supabase status`.
#
# Só chave pública aqui. A service role key não entra em variável NEXT_PUBLIC_,
# não entra neste arquivo e não entra no repositório.
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=
